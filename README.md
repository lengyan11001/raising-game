# CloudToken

CloudToken is a Node.js web app with:

- public user pages
- admin dashboard
- account and credit management
- generation history
- model pricing configuration
- upstream generation API integration

This handoff package is sanitized. It does not include production secrets,
database data, uploaded user files, generated results, logs, or server access
credentials.

See `DEPLOY.md` for deployment steps.
